SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
EXECUTE_DIR="$(cd -P "$(dirname "${BASH_SOURCE[0]}")/../..";pwd)"

start_game() {
	source "$EXECUTE_DIR/.settings"
	am start -S -n $GAME_NAME/com.huuuge.casino.BootActivity --es args '"-v full -o /sdcard/test.vktrace"'
}
